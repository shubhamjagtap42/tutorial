import { ChangetxtDirective } from './changetxt.directive';

describe('ChangetxtDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangetxtDirective();
    expect(directive).toBeTruthy();
  });
});
