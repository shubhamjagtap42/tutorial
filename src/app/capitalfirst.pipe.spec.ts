import { CapitalfirstPipe } from './capitalfirst.pipe';

describe('CapitalfirstPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalfirstPipe();
    expect(pipe).toBeTruthy();
  });
});
